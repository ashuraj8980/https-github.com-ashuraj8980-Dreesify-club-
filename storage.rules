rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated users can write (upload) to the 'products' folder.
    match /products/{allPaths=**} {
      allow read; // Anyone can read images
      allow write: if request.auth != null;
    }
  }
}
